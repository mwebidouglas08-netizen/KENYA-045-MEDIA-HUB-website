[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --production"]

[phases.build]
cmds = [
  "mkdir -p public",
  "[ -f index.html ] && cp index.html public/index.html || true"
]

[start]
cmd = "node server.js"
