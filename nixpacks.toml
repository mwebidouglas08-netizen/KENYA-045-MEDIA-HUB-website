[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --production"]

[phases.build]
cmds = ["mkdir -p public", "cp index.html public/index.html"]

[phases.start]
cmd = "node server.js"
